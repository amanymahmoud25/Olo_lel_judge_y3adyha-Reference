vector<int>color ;
vector<vector<int>> adj;
bool bi = 1 ;
void dfs(int node , int col) {
   color[node]=col ;
   for (int i:adj[node]) {
       if (i == node) continue;




      if (color[i]==col)
          bi=0 ;


        if (color[i]==-1)
       dfs(i,!col) ;
   }
}
