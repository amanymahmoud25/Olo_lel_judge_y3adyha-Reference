Check loops in undirected graph
vector<bool>visited ;
vector<vector<int>> adj;
bool cycle = false ;
void dfs(int node , int parent) {
   visited[node]=1 ;
   for (int i:adj[node]) {
       if (i!=parent && visited[i]) {
           cycle=1 ;
          // cout << node << endl;
       }
       else dfs(i , node) ;
   }
}
   void solve() {
   int n ,m  ; cin>>n >>m ;
       visited.assign(n+1,0) ;
       adj.assign(n+1 , vector<int>()) ;
   for (int i=0 ; i<m  ; i ++) {
       int x, y ; cin >>x >>y ;
       adj[x].push_back(y) ;
       adj[y].push_back(x) ;
   }
   dfs(1,0);
   cout << cycle << endl;


   }

Check loops in Directed graph

bool cycle = false ;
void dfs(int node , int parent) {
   visited[node]=2 ;
   for (int i:adj[node]) {
      if (visited[i]==2)
          cycle=1 ;


       if (visited[i]) continue;


       dfs(i,node) ;
   }
   visited[node]=1;
}
void solve() {
   int n ,m  ; cin>>n >>m ;
   visited.assign(n+1,0) ;
   adj.assign(n+1 , vector<int>()) ;
   for (int i=0 ; i<m  ; i ++) {
       int x, y ; cin >>x >>y ;
       adj[x].push_back(y) ;
    //   adj[y].push_back(x) ;
   }
   dfs(1,0);
   cout << cycle << endl;




}


