** Binary Search Integers

ll l=0,r=1e9, md,ans=0;

auto valid=[&](ll md){

    return (condition);
};
while(l<=r){
    //cout<<md<<endl;
    md=l+(r-l)/2;
    if(valid(md)){
        ans=max(md,ans);
        l=md+1;

    }
    else r=md-1;
}
cout<< ans<<endl;

-------------------------------------------------------------------------

** Binary Search Double


 ld l=0,r=1e12,ans=0;
    int itr = 400;
 
    while( itr -- && l<=r){
        ld md=l+(r-l)/2;
    
        ld res = valid(md) ;
       if (res <k )
        {
            l=md ;
        }
        else r=md;
    }

     // في النهاية l و r متطابقان تقريباً، نطبع أيهما
    cout <<fixed<<setprecision(6) <<l <<endl;
